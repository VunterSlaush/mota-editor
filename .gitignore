node_modules/
dist/

# This repo uses npm (see AGENTS.md). A stray pnpm/yarn lockfile means two
# dependency graphs and a lockfile that nobody updates.
pnpm-lock.yaml
yarn.lock

# The Jev example keeps its API key beside its code.
examples/jev/config.json

src-tauri/target/
src-tauri/gen/schemas/
*.log
.DS_Store
