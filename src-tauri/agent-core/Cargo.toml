[package]
name = "agent-core"
version = "0.1.0"
edition = "2021"
description = "Provider-agnostic agent domain: turn commands and event-stream parsing. No Tauri, no I/O."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
